{
  "name": "@wizard/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --external:fastify --external:@fastify/static --external:socket.io --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/static": "^7.0.4",
    "@wizard/shared": "*",
    "fastify": "^4.28.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "esbuild": "^0.24.2",
    "tsx": "^4.19.2"
  }
}
